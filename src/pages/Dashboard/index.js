import React from 'react';
import {Row} from 'antd';

const Dashboard = () => {
        return (
                <React.Fragment>
                        <Row wrap style={{marginTop: 20}}>
                                WelCome
                        </Row>
                </React.Fragment>
        );
};

export default Dashboard;
